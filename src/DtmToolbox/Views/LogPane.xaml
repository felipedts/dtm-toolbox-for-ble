<UserControl x:Class="DtmToolbox.Views.LogPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:DtmToolbox.ViewModels">
    <!-- Lines are selected like in a file list: click, Shift+click, Ctrl+click, Ctrl+A. Ctrl+C copies them. -->
    <ListBox x:Name="LogList" ItemsSource="{Binding Log}" SelectionMode="Extended"
             Background="{StaticResource Gray900Brush}" BorderThickness="0" Padding="12,8"
             FontFamily="Consolas" FontSize="12" HorizontalContentAlignment="Stretch"
             ScrollViewer.HorizontalScrollBarVisibility="Auto"
             VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
        <ListBox.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Copy" InputGestureText="Ctrl+C" Command="ApplicationCommands.Copy"
                          CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                <MenuItem Header="Copy all" Click="OnCopyAllClick" />
                <Separator />
                <MenuItem Header="Select all" InputGestureText="Ctrl+A" Command="ApplicationCommands.SelectAll"
                          CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
            </ContextMenu>
        </ListBox.ContextMenu>
        <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Row" Background="Transparent" Padding="0,1">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Row" Property="Background" Value="{StaticResource Gray700Brush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ListBox.ItemContainerStyle>
        <ListBox.ItemTemplate>
            <DataTemplate DataType="{x:Type viewModels:LogEntry}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Time}" Foreground="{StaticResource Gray500Brush}" Margin="0,0,12,0" />
                    <TextBlock x:Name="Message" Text="{Binding Message}" Foreground="{StaticResource Gray100Brush}" />
                </StackPanel>
                <DataTemplate.Triggers>
                    <DataTrigger Binding="{Binding Kind}" Value="Frame">
                        <Setter TargetName="Message" Property="Foreground" Value="{StaticResource Gray400Brush}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Kind}" Value="Error">
                        <Setter TargetName="Message" Property="Foreground" Value="#EF9A9A" />
                    </DataTrigger>
                </DataTemplate.Triggers>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</UserControl>
